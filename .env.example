# Copy this file to `.env` for local development. Do NOT commit real secrets.
# On Netlify/Cloudflare/etc: add these as Environment variables in your project settings.

########################################
# Private (server/build) environment
########################################

# Sentry auth token used by Sentry's build tooling (e.g., Vite plugin) to create releases and upload source maps.
# NEVER expose this on the client. Keep only in server/build environment.
# Example format: sntrys_...
SENTRY_AUTH_TOKEN=

########################################
# Public (client-exposed) environment
########################################
# Any variable prefixed with PUBLIC_ is embedded into the client bundle.

# Formspark form ID (from your dashboard). Example submit URL will be:
#   https://submit-form.com/${PUBLIC_FORMSPARK_FORM_ID}
PUBLIC_FORMSPARK_FORM_ID=

# Cloudflare Turnstile site key for this domain (NOT the secret key).
# Create a Turnstile widget in Cloudflare Dashboard → Turnstile and copy the Site key.
PUBLIC_TURNSTILE_SITE_KEY=
